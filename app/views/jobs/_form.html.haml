= simple_form_for(@job, html: { class: 'form-horizontal'}) do |f|
  =f.collection_select :category_id, Category.all, :id, :name, {prompt "Choose a job category"}
  =f.input :title, label: "Job Title"
  =f.input :content, label: "Job Description"
  %br/
  =f.button :submit